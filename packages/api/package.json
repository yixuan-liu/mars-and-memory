{
  "name": "@mars-memory/api",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mars-memory/types": "workspace:*",
    "zod": "^3"
  },
  "devDependencies": {
    "@mars-memory/config": "workspace:*",
    "typescript": "^5"
  }
}
